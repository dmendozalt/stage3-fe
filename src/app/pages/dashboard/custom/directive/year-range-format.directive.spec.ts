import { YearRangeFormatDirective } from './year-range-format.directive';

describe('YearRangeFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new YearRangeFormatDirective();
    expect(directive).toBeTruthy();
  });
});
