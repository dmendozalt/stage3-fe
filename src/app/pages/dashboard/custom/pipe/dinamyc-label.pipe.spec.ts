import { DinamycLabelPipe } from './dinamyc-label.pipe';

describe('DinamycLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new DinamycLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
